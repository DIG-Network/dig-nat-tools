/**
 * Registry Module
 * 
 * Exports connection registry functionality and types.
 */

export * from './types';
export { ConnectionRegistry, connectionRegistry } from './connection-registry'; 